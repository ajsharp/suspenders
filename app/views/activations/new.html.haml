- @title = "Set your password"

%h1 Almost done...

%p You'll need a password for this site. Please create one now.

-semantic_form_for(@user, :url => activate_path(@user.id), :method => :post)  do |f|
  = f.error_messages
  = render :partial => "password_resets/form", :locals => {:f => f}
  -f.buttons do
  =f.commit_button "Activate Account!"
